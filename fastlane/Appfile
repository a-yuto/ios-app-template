# Apple ID / Team / Bundle ID は環境ごとに上書きしてください
# .env (gitignore対象) に書くか、CI環境変数で渡すのを推奨

app_identifier(ENV["APP_IDENTIFIER"] || "niki.ios-app-template")
apple_id(ENV["FASTLANE_APPLE_ID"])
team_id(ENV["FASTLANE_TEAM_ID"] || "4L2W8Y4RLS")
